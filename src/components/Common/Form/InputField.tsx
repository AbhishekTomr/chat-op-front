import React from 'react';
import { TextField } from '@mui/material';

interface ITextField{
  label: string;
}

const InputField:React.FC<ITextField> = (props:ITextField) => {
  return (
    <div>
      
    </div>
  )
}

export default InputField;
